version: 0.1
phases:
  install:
    commands:
      - make
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket alejandroquesada.com --output-template-file NewSamTemplate.yaml
artifacts:
  type: zip
  files: 
    - NewSamTemplate.yaml